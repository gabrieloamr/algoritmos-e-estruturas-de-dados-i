#include <stdio.h>
#include <stdlib.h>

int main() {
    int contador = 1;

    while (contador <= 10) {
        printf("%d\n", contador);
        contador += 1;
    }
    
    return 0;
}